# DEVLOG.md — 서울시 개별공시지가 정보 MCP

기록 형식:
```
## YYYY-MM-DD
- 진행 내용:
- 확인 필요 / 실측 결과:
- 문제 및 해결:
```

---

## 2026-08-16
- 진행 내용: DEVPLAN.md/CLAUDE.md/README.md/DEVLOG.md 4종 문서 생성 완료 (Claude 웹챗 단계)
- 확인 필요 / 실측 결과:
  - START_INDEX/END_INDEX로 실제 여러 건이 반환되는지 미확인 (명세서상으로는 최대 1000건 차이까지 가능하다고 되어 있으나 실제 API 동작은 로컬 테스트 단계에서 실측 필요)
  - BASE_MON(기준년월) 필드 포맷(YYYYMM 추정) 실측 필요
- 문제 및 해결: 해당 없음 (아직 구현 전)

## 2026-08-16 (구현 단계)
- 진행 내용:
  - requirements.txt, seoul_api.py, server.py 작성 완료 (`mcp.run(..., stateless_http=True)` 반영)
  - .venv 생성 후 fastmcp/httpx/python-dotenv 설치
  - FastMCP 서버 임포트 및 `list_tools()` 스모크 테스트 통과 — 툴 1개(`get_individual_land_price`)만 등록됨 확인
  - Dockerfile, fly.toml 작성 완료
- 확인 필요 / 실측 결과 (⚠️ 미해결):
  - 실제 `.env`의 `SEOUL_API_KEY`(30자리 hex 형태)로 `IndividuallyPostedLandPriceService` 호출 시
    **일관되게 `ERROR-500` (서버 오류) 반환** — 동일 요청을 3회 재시도했으나 동일하게 실패 (CLAUDE.md 3회 재시도 규칙에 따라 중단).
  - URL 경로 구조는 DEVPLAN.md 스펙대로 11개 세그먼트(KEY/json/IndividuallyPostedLandPriceService/START/END/SIGUNGU_NM////  /2020)를 구성했고,
    `sample` 키로도 동일 패턴 테스트 시 `ERROR-300`(필수값 누락)이 발생해 URL 구조 자체에 대한 확신이 낮음.
  - 서비스명 대소문자를 바꿔보면(`individuallyPostedLandPriceService`) 에러 코드가 `ERROR-300`→`ERROR-500`으로 달라지는 것을 확인 — 이는 API가 요청을 다르게 처리하고 있다는 신호이나 근본 원인은 특정하지 못함.
  - **가능성 있는 원인 (실측 불가, 사용자 확인 필요)**:
    1. `.env`의 키가 이 특정 서비스(IndividuallyPostedLandPriceService)에 대해 활성화되지 않았을 가능성 (서울 열린데이터광장은 서비스별로 키를 재승인해야 하는 경우가 있음)
    2. 키 형식이 일반적인 서울 열린데이터광장 키(24자 영숫자)와 달리 30자리 hex 형태로 보여, 다른 시스템의 키가 잘못 저장되었을 가능성
    3. START_INDEX/END_INDEX 실측(여러 건 반환 여부), BASE_MON 포맷 실측은 이 오류로 인해 **아직 수행하지 못함**
  - **코드 자체는 DEVPLAN.md 스펙대로 구현 완료**되어 있으며, 유효한 키로 재시도하면 바로 검증 가능한 상태.
- 문제 및 해결:
  - 문제: 실제 API 호출이 ERROR-500으로 지속 실패해 END-TO-END 실측을 완료하지 못함.
  - 해결(보류): 사용자가 서울 열린데이터광장에서 `IndividuallyPostedLandPriceService`용 키가 정상 승인되었는지 확인 후 재테스트 필요. 코드 수정 없이 유효한 키만 있으면 바로 검증 가능.

## 2026-08-16 (URL 인코딩 가설 검증)
- 진행 내용:
  - `seoul_api.py`에 `urllib.parse.quote(part, safe="")`를 적용해 모든 경로 세그먼트(SIGUNGU_NM 등 한글 포함)를 명시적으로 URL 인코딩하도록 수정.
  - `.env`의 `SEOUL_API_KEY` 확인 — 이전과 동일한 30자리 키(변경 없음, BOM 없음, 44바이트).
  - 인코딩 적용 후 동일 요청(`종로구`, `2020`, START=1/END=5) 재실측.
- 확인 필요 / 실측 결과:
  - **가설 기각**: URL 인코딩 문제가 아니었음. 인코딩 적용 전/후 요청 URL을 직접 비교한 결과 httpx가 이미 한글 경로를 자동으로 percent-encoding 처리하고 있었고(`%EC%A2%85%EB%A1%9C%EA%B5%AC` 동일하게 생성됨), 명시적 `quote()` 적용 후에도 응답은 **동일하게 `ERROR-500`**.
  - 즉 인코딩 방식과 무관하게 서버가 동일한 오류를 반환 — 이전 로그(2026-08-16 구현 단계)에서 추정한 "키가 이 서비스에 대해 미승인 상태일 가능성"이 여전히 가장 유력한 원인으로 남음.
  - START_INDEX/END_INDEX 다건 반환 실측, BASE_MON 포맷 실측은 여전히 **미완료** (ERROR-500이 선행 차단).
- 문제 및 해결:
  - 문제: URL 인코딩을 명시적으로 적용해도 ERROR-500 지속.
  - 해결: 코드 변경(quote 적용)은 유지 — 안전성 측면에서 문제없고 향후 특수문자 포함 법정동명 등에도 안전. 단, 근본 원인은 API 키 승인 상태 확인이 필요하므로 사용자가 서울 열린데이터광장에서 직접 확인 필요.

## 2026-08-16 (TYPE=xml 가설 검증)
- 진행 내용: 동일 요청(`종로구`, `2020`, START=1/END=5)을 TYPE만 `json`→`xml`로 바꿔 실제 키로 재실측 (JSON 파싱 문제 여부 확인 목적, server.py/seoul_api.py는 TYPE=json 고정이므로 이 테스트는 임시 스크립트로만 수행, 코드 변경 없음).
- 확인 필요 / 실측 결과:
  - **가설 기각**: TYPE=xml로도 동일하게 `ERROR-500` 발생 (`<CODE>ERROR-500</CODE>`). JSON 파싱/역직렬화 문제가 아니라 **서버가 요청 자체를 처리하는 단계에서 발생하는 오류**임을 확인.
  - 응답 포맷(json/xml)과 무관하게 동일 오류가 재현되므로, 원인은 클라이언트 측 파싱이 아니라 요청(키 승인 상태, 서비스 활성화 여부 등) 쪽에 있을 가능성이 더 높아짐.
  - START_INDEX/END_INDEX 다건 반환 실측, BASE_MON 포맷 실측은 여전히 **미완료**.
- 문제 및 해결:
  - 문제: TYPE 변경으로도 ERROR-500 지속, JSON 파싱 문제 가설 기각.
  - 해결: CLAUDE.md의 "동일 오류는 최대 3회까지만 재시도" 원칙에 따라 이 API 자체에 대한 추가 가설 검증(인코딩, TYPE)은 중단. 남은 유력 원인은 API 키의 서비스별 승인 상태이며, 이는 서울 열린데이터광장 마이페이지에서 사용자가 직접 확인해야 함. 코드는 TYPE=json 고정 유지(DEVPLAN.md 스펙).

## 2026-08-16 (키 승인 완료 후 재테스트)
- 진행 내용: 사용자가 서울 열린데이터광장에서 `IndividuallyPostedLandPriceService`에 대한 인증키 승인 완료 (기존 `.env`의 키와 동일한 키를 그대로 승인, 신규 키 발급 아님). `.env` 파일 크기(44바이트) 변동 없음 확인 후 동일 요청(`종로구`, `2020`, START=1/END=5, TYPE=json)으로 재실측.
- 확인 필요 / 실측 결과:
  - **여전히 `ERROR-500` 발생.** 키 승인 직후에도 즉시 반영되지 않았을 가능성 있음 — 서울 열린데이터광장은 승인 후 서버 반영까지 지연이 있는 경우가 있다고 알려져 있음(실측 근거 아님, 사용자 확인 필요).
  - START_INDEX/END_INDEX 다건 반환 실측, BASE_MON 포맷 실측 여전히 **미완료**.
- 문제 및 해결:
  - 문제: 키 승인 후에도 ERROR-500 동일 재현.
  - 해결(보류): 잠시 후(예: 수 분~수십 분) 재시도 필요. 반복되면 서울 열린데이터광장 Q&A로 직접 문의 권장(에러 메시지 자체가 이를 안내하고 있음). Claude Code 측 코드/URL 구조 문제는 아닌 것으로 재확인됨(인코딩·TYPE 가설 모두 기각 완료).

## 2026-08-16 (신규 키 발급 후 근본 원인 규명 — ERROR-500 해결)
- 진행 내용:
  - 사용자가 신규 키(`6b6c764c...`)를 발급받아 전달, `.env`를 UTF-8(BOM 없음)로 갱신 (44바이트, 기존과 다른 값으로 교체 확인).
  - 새 키로 동일 요청(선택 파라미터 전부 빈 문자열) 재실측 → **여전히 `ERROR-500`**.
  - 하지만 SIGUNGU_NM/YEAR만 채운 최소 요청(선택 파라미터 세그먼트 자체를 생략)은 `ERROR-300`(필수값 누락)으로 정상 처리됨을 재확인 → 새 키 자체는 유효하고, 서버가 요청을 정상적으로 구분하고 있음을 확인.
  - 파라미터 조합을 단계적으로 좁혀 실측한 결과:
    - `종로구/////2020` (선택 4개 전부 빈 문자열) → `ERROR-500`
    - `종로구/2020` (선택 세그먼트 자체 생략, 개수 불일치) → `ERROR-300`
    - `종로구/청운동/1/1/1/2020` (선택 4개 전부 실제 값) → **`INFO-000` 정상 처리, 실제 데이터 반환 확인** (`BJDONG_NM=청운동, BONBEON=0001, BUBEON=0001, PILGI_CD=1, BASE_MON=2020-01-01, JIGA=1392000, YEAR=2020`)
    - `종로구/청운동//// 2020` (일부만 채움, 예: bubeon만 비움) → `ERROR-500`
  - **근본 원인 규명 완료**: 이 API는 DEVPLAN.md가 가정한 "선택 파라미터를 빈 문자열로 채워 자리만 유지" 방식을 지원하지 않는다. `BJDONG_NM/BONBEON/BUBEON/PILGI_CD`는 **넷 다 채우거나 넷 다 세그먼트를 생략**해야 하며, 하나라도 부분적으로 채우거나 "빈 문자열로 자리만 채우는" 방식은 서버에서 `ERROR-500`을 유발한다.
  - `seoul_api.py`에 클라이언트 측 사전 검증 로직 추가: 4개 선택 파라미터 중 일부만 채워졌으면 API 호출 전에 `ERROR-CLIENT-PARTIAL-PARAMS`로 명확한 안내 메시지를 반환하도록 수정. `server.py` docstring에도 이 제약 명시.
  - JIGA(공시지가) 실측값 확인: `1392000` (원/㎡ 단위, DEVPLAN.md 명세와 일치). BASE_MON 포맷도 `YYYY-MM-DD`(예: `2020-01-01`)로 실측 확인 — DEVPLAN.md의 "YYYYMM 추정"과 다름, **BASE_MON은 YYYY-MM-01 형태**로 갱신 필요.
- 확인 필요 / 실측 결과:
  - ✅ ERROR-500 원인 규명 및 해결 완료 (클라이언트 측 검증으로 우회).
  - ✅ JIGA 단위(원/㎡) 실측 일치 확인.
  - ⚠️ BASE_MON 포맷: DEVPLAN.md의 "YYYYMM 추정"이 아니라 실측상 `YYYY-MM-DD`(`2020-01-01`) 형태였음. README.md/DEVPLAN.md에 반영 필요.
  - ⚠️ START_INDEX/END_INDEX로 실제 여러 건이 반환되는지는 **여전히 미확인**. 선택 파라미터 4개를 모두 채워야 유효 요청이 되는데, 본번/부번까지 특정하면 필지 단위로 좁혀져 통상 1건만 반환되는 구조로 보임(`list_total_count=1`). 여러 건을 얻으려면 본번/부번에 와일드카드 성격의 값(예: `0`)이 있는지 추가 탐색이 필요하나, 종로구 청운동/강남구 역삼동 등에서 본번=0/부번=0 조합은 `INFO-200`(데이터 없음)만 반환되어 아직 다건 반환 사례를 실측하지 못함. CLAUDE.md 3회 재시도 원칙에 따라 이 탐색은 여기서 중단.
- 문제 및 해결:
  - 문제: 선택 파라미터를 빈 문자열로 채우는 DEVPLAN.md 설계가 실제 API와 맞지 않아 ERROR-500 유발.
  - 해결: `seoul_api.py`에 부분 채움 방지 검증 로직 추가(전부 채움 또는 전부 생략만 허용), `server.py` docstring에 사용자 안내 추가. 근본 원인은 클라이언트 코드가 아니라 API 자체의 동작 방식이었으므로 URL 구조/인코딩은 원래 스펙(DEVPLAN.md 경로 순서) 그대로 유지.
  - 남은 미해결: START/END 다건 반환 실측 — 사용자가 실제 다건이 존재하는 (시군구, 법정동, 본번, 부번, 필지구분코드) 조합을 알고 있다면 제공 시 추가 검증 가능.

## 2026-08-16 (시군구+연도만으로 다건 조회 시도)
- 진행 내용: BJDONG_NM/BONBEON/BUBEON/PILGI_CD 4개를 전부 빈 문자열로 두고 SIGUNGU_NM+YEAR만으로 조회 시 `list_total_count > 1`이 나오는지 재확인. `seoul_api.py`를 통한 호출과 원시 URL 요청(`.../종로구/////2020`, START=1/END=10) 두 방식으로 각각 실측.
- 확인 필요 / 실측 결과:
  - **재현됨: 이 조합은 `ERROR-500`을 반환**하며 `list_total_count`를 확인할 수 없음. 이전(2026-08-16 근본 원인 규명 로그)에서 확인한 "선택 파라미터 4개 중 하나라도 빈 문자열이면 ERROR-500" 패턴과 동일.
  - 결론: **시군구명+연도만으로 여러 건을 조회하는 것은 이 API에서 지원되지 않는 것으로 보임** — DEVPLAN.md 2절("페이징 방식")이 가정한, SIGUNGU_NM+YEAR만으로 START_INDEX/END_INDEX 범위의 여러 건을 받는 시나리오는 실측상 성립하지 않음.
  - START_INDEX/END_INDEX가 실제로 다건을 반환하는지는 여전히 **미확인 상태로 남음**. 4개 선택 파라미터를 모두 채운 유효 요청만 가능한데, 본번/부번까지 특정하면 필지 단위로 좁혀져 통상 1건(`list_total_count=1`)만 나오므로, 이 API의 START/END 페이징이 실제로 유효하게 동작하는 시나리오 자체를 아직 찾지 못함.
- 문제 및 해결:
  - 문제: DEVPLAN.md가 가정한 "시군구+연도 기준 다건 조회 후 START/END로 페이징" 시나리오가 실제 API 동작과 다름.
  - 해결(보류): 코드 수정 없음 — `seoul_api.py`의 검증 로직은 이미 이 조합(부분/전체 빈 문자열)을 정상적으로 API까지 전달하고 있어 추가 수정 불필요. 근본적으로 이 API가 어떤 조합에서 다건을 반환하는지는 사용자가 알고 있는 실제 다건 존재 사례(예: 특정 법정동에 필지구분코드만 다르고 본번/부번이 같은 여러 필지가 있는 경우 등)를 제공받아야 추가 검증 가능. README.md/DEVPLAN.md의 "다건 페이징 가능" 서술은 이 실측 결과를 반영해 재검토가 필요할 수 있음.

## 2026-08-16 (Rate Limiting 추가)
- 진행 내용:
  - 사용자 요청에 따라 3단계 rate limit을 `server.py`에 in-memory로 구현.
    - 규칙 1: 같은 IP 기준 분당 3회 초과 시 429
    - 규칙 2: 1시간 내 429를 5회 이상 받은 IP는 이후 24시간 차단
    - 규칙 3: IP당 일일(rolling 24h) 총 호출 30회 초과 시 429
  - 구현 방식: 최초에는 raw ASGI 미들웨어(`starlette`)로 접근했으나, FastMCP가 `fastmcp.server.middleware.Middleware` 베이스 클래스(`on_call_tool` 훅)와 `get_http_request()`(요청 컨텍스트에서 Starlette `Request` 획득)를 자체 제공하는 것을 확인하고 이를 사용하도록 재작성. 툴 호출(`tools/call`) 시점에 IP를 확인해 한도를 초과하면 `fastmcp.exceptions.ToolError`를 발생시켜 `429: ...` 메시지로 MCP 클라이언트에 오류가 전달되도록 함.
  - 클라이언트 IP는 `X-Forwarded-For` 헤더(fly.io 프록시 경유 시) 우선, 없으면 `request.client.host`로 판별.
  - 카운터는 `collections.deque` 기반 sliding window(분당/일일/1시간 429 카운트) + `threading.Lock`으로 스레드 안전성 확보.
  - 로컬에서 `server.mcp._check_rate_limit(ip)`를 직접 호출하는 단위 테스트로 3개 규칙 모두 동작 확인(규칙1: 4번째 호출에서 차단, 규칙2: 429 5회 후 추가 요청이 차단 메시지로 응답, 규칙3: 40회 요청 중 30회만 통과·10회 429).
- 확인 필요 / 실측 결과:
  - ⚠️ in-memory 저장이므로 fly.io가 머신을 2대 이상 띄우면(기본 고가용성 설정) 머신별로 카운터가 분리되어 완전한 전역 제한이 아님 — CLAUDE.md 지침에 따라 in-memory로 충분하다고 판단해 별도 외부 저장소(Redis 등)는 추가하지 않음. README.md에 이 한계를 명시함.
  - 실제 배포 환경(fly.io, 다중 IP, 실제 429 발생 시 클라이언트에 노출되는 메시지 형태)은 로컬 테스트만으로는 완전히 검증 불가 — 배포 후 사용자가 직접 확인 필요.
- 문제 및 해결:
  - 문제: 처음에는 raw ASGI 미들웨어로 구현을 시도했으나, FastMCP의 `mcp.run(transport="streamable-http", ...)` 호출 방식에서는 ASGI 앱을 직접 감싸는 지점이 명확하지 않아 FastMCP 자체 미들웨어 시스템(`add_middleware`)으로 전환.
  - 해결: `Middleware.on_call_tool` 훅 사용으로 해결 — 툴 호출마다 실행되며 `get_http_request()`로 클라이언트 IP를 얻을 수 있음을 확인. 배포는 수행하지 않음(CLAUDE.md 지침에 따라 `flyctl deploy`는 사용자가 직접 실행).
